[package]
name = "extension_api"
version = "0.2.0"
edition = "2024"
publish = false
license = "MIT"
description = "Native rapira extension contract - the API every extension implements to drive PHP"

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
# Master-side pre-fork socket binding (sync syscalls; keeps this crate tokio-free).
socket2 = "0.6"

[dev-dependencies]
libc = "0.2"
