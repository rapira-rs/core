; Timeout suite config: a 1s execution cap so the spin fixture dies fast, and
; visible errors so the timeout fatal lands in the response body deterministically.
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
max_execution_time = 1
max_input_time = -1
